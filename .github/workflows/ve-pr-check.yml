name: VE PR Check

on:
  pull_request:
    branches: [ "main" ]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: VE PR verify stub
        run: |
          echo "VE PR verify stub running..."
          echo "TODO: wire in extra checks (docs, schema, etc.)."
